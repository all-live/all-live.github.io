import type { NextPage } from 'next';
import Header from '@components/Header';
import Layout from '@layout';

const Home: NextPage = () => (
  <Layout>
    <Header />
  </Layout>
);

export default Home;
